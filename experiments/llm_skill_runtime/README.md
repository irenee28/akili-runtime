# LLM skill-runtime development lineage

This directory contains the original Kimi-produced v0.1 and v0.2 standalone notebooks.

The corresponding uploaded result files are under `../../results/development/kimi_llm_skill_runtime/`. Both original runs have `all_passed: false`; they are retained for reproducibility and audit history, not promoted as successful flagship evidence.
