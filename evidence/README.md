# Evidence assets

This directory contains frozen notebooks, verification files, console logs and reproducibility packages used by the public results summary.

The evidence is intentionally organised by protocol. Do not combine the V4 generation result with V4-MA-E2.2 transport.

Run `sha256sum -c SHA256SUMS` from the repository root to validate the packaged files.
